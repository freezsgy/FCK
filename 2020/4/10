今日汇报：
这篇文章根据联邦学习服务端的聚合规则进行攻击。主要有三种聚合规则：1）Krum：Krum选择与其他模型相似的m个局部模型中的一个作为全局模型。
2）Trimmed mean：此聚合规则独立地聚合每个模型参数。具体来说，对于每个本地模型第j个模型参数，主设备对m个本地模型的第j个参数进行排序，去掉排序后的
前n和后n个参数，然后求均值，作为全局模型的第j个模型参数。
3）Median：对于每个本地模型第j个模型参数，主设备对m个本地模型的第j个参数进行排序，选取中位数作为全局模型的第j个模型参数。
根据这些聚合规则进行本地模型投毒攻击部分还没有看明白，明天继续看这一部分和实验部分。
