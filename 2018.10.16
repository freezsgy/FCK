1.今天上午去售后修电脑去了

2.今天看了针对多分类的方程求解攻击，假设多分类要完成对c个类别分类，置信度则是输入在每个类别的概率分布，输出的置信度是n维向量。 
其未知参数有c*(n+1)（c个类别，每个类别存在n+1个未知数），当然这个函数(fi(x)=exp(wi*x+bi)/[exp(w0*x+b0)+exp(w1*x+b1)+...+exp(wj*x+bj)],
其中bj=c-1)是非线性函数，也就是说，通过多次访问构建的方程组是非线性方程组，而且每个方程都是超越方程。关于这种方程组的求解可
以利用梯度下降方法实现，构造一个损失函数为凸函数，转化为凸优化问题求解，求解全局最优解也就是该模型的未知参数。

3.决策树提取攻击。决策树的置信度是在给定训练数据集，就已经确定了，每个叶子节点都有其对应的置信度值（这个置信度值就是分类id），
笔者在此假设每个叶子节点都有不同的置信度值，也就说可以通过置信度标定不同的叶子节点：利用置信度作为决策树叶子节点的伪标识。同时
很多MLaaS提供商做为了提升APIs访问的可访问性，MLaaS提供商的做法是即使输入的数据使部分特征依然可以得到输出结果。
但是要基于两点假设：
（1）每个叶子节点都有不同的置信度值，即分类类别id各不相同。
（2）为了提升APIs的可访问性（感觉相当于模糊查询，不用给出所有特征值就可以得到分类结果），Mlaas提供商设置提供输入缺省特征向量
（只有部分特征值）也可得出结果。
