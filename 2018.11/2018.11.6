1.今天学习了PHP教学视频，完成了apache+php+mysql的服务器环境的搭建。
2.看论文，理了一遍影子模型训练数据的合成方法，着重于算法一的理解：
算法1：基于模型的影子训练数据的合成：（x*，y*）即为成功合成的数据
/* 一个数据合成的过程 */
首先程序开始：固定一个c类，合成尽可能预测为c类（即预测向量中c类的概率最大）的训练数据。
随机初始化x，初始化yc*=0；j=0；k=k"max" 。
迭代开始，1<=迭代次数<=itermax（）
将x输入目标模型，输出y
判断预测向量y中的类c概率yc是否大于等于yc*，若是，则
		判断yc是否大于等于阈值且类c的概率是否是y向量中最大的，若是，则
			判断yc是否大于一个随机数（0到1之间），若是，则
				就直接返回x（成功合成的数据）
		x*=x；yc*=yc；j=0。//此为每一次迭代的结果
	若不是，则
		j=j+1；
		判断j是否大于设定的j的最大值，若是，则
			k = kmin和k/2（向上取整）中的的最大值
			j=0
x=随机化x*（只随机化x*中的k个特征）
返回合成数据失败
